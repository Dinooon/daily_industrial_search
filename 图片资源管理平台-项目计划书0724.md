# 项目计划书：设计情报与视觉资产管理平台

## 1. 项目背景与平台定位

当前工业设计、产品及创新团队在日常灵感搜集、竞品分析和视觉素材积累中，常面临数据零散、检索困难、重复劳动及知识无法沉淀等痛点。

本平台定位为**面向内部团队的设计情报与视觉资产管理中枢**。平台区别于普通的资讯聚合器或单纯的云盘，其核心价值在于构建数据的**结构化关联**，将资讯文章、图片资产、标签体系、项目图库、版权来源及用户行为深度绑定，形成一个可持续生长的团队共有知识库。

---

## 2. 总体目标与业务蓝图

### 2.1 总体目标
利用自动化抓取、AI 视觉解析与向量检索技术，建设一个可持续运行的内部私有化平台。实现设计情报的每日自动入库、团队无缝共享、视觉资产的高效检索与智能分类。

### 2.2 核心业务指标
1. **自动化侦测**：每日自动监测首批 10～20 个核心设计类网站，精准提取新增图文，避免重复抓取。
2. **结构化提取**：自动解析文章标题、摘要、发布时间、主图及辅助图片，完整保留原始来源、作者与版权溯源信息。
3. **资产标准化**：对入库图片执行自动下载、特征去重，并生成适用于 Web 端快速加载的缩略图。
4. **多维检索与调用**：支持团队按分类、风格、材质、颜色、来源及关联项目进行条件筛选与高速检索。
5. **智能扩展能力**：支持用户自主上传拍摄素材，预留 AI 自动打标与自然语言“以意搜图”功能接口。

---

## 3. 技术架构设计

为确保平台的稳定性、扩展性及便于在部门公共电脑上进行局域网部署，系统采用前后端分离与容器化的标准微服务架构。

| 模块层级 | 技术选型与功能描述 |
| :--- | :--- |
| **前端交互层 (Frontend)** | **Vue.js / React**<br>提供极简、硬朗的瀑布流 UI 界面；支持拖拽上传、画廊模式预览、多维标签筛选及项目文件夹管理。 |
| **后端逻辑层 (Backend)** | **Python (FastAPI / Django)**<br>处理前端请求，管理用户权限验证（RBAC），调度系统任务及响应 AI 处理队列。 |
| **数据抓取层 (Pipeline)** | **Scrapy / Playwright + Celery**<br>针对静态与动态网页的爬虫引擎，结合定时任务调度，执行每日数据增量拉取与 pHash 图片去重。 |
| **数据存储层 (Storage)** | **PostgreSQL**：存储文章、标签、项目及用户等结构化关系数据。<br>**MinIO**：私有化对象存储，专用于存放高清原图与缩略图文件。 |
| **AI 计算层 (预留)** | **大视觉模型 API / CLIP**<br>通过消息队列异步处理图像，输出材质/风格标签的 JSON 数据，并提取特征向量。 |
| **向量检索层 (预留)** | **Milvus / Qdrant**<br>存储图像特征向量，支撑后期的自然语言检索（以意搜图）需求。 |
| **运行与部署环境** | **Docker & Docker Compose**<br>环境全容器化打包。前期在本地开发，后期一键迁移至部门公共电脑，通过局域网 IP 供全员使用。 |

---

## 4. 核心功能规划与实施路径

项目采取“小步快跑，敏捷迭代”的策略，分三个阶段推进落地：

### 阶段一：数据获取与基础呈现（MVP 最小可行性产品）
*核心目标：跑通从外部网站到内部呈现的自动化闭环。*
*   搭建 PostgreSQL 数据库与 MinIO 图片存储基建。
*   编写 10-20 个目标网站的爬虫脚本，实现定时触发与增量更新。
*   构建前端瀑布流资讯页，实现基础的文章/图片展示、来源信息回溯及按网站分类的筛查。
*   使用 Docker 完成本地到部门局域网的首次部署测试。

### 阶段二：用户交互与资产沉淀（内部工具化）
*核心目标：引入用户角色，建立图片与业务项目的结构化关联。*
*   开发用户账号系统与权限隔离机制。
*   上线用户自定义项目文件夹（Project库），支持前端交互式收藏图片至特定项目。
*   开放外部图片上传接口，允许设计师将实拍照片或本地资产汇入平台。
*   建立后台管理面板，监控爬虫运行状态、失败日志及数据源健康度。

### 阶段三：AI 赋能与高级检索（智能化跃迁）
*核心目标：用 AI 替代人工整理，大幅降低检索成本。*
*   **AI 自动化标签处理流**：引入异步任务队列，当图片（抓取或用户上传）入库时，调用 AI 模型自动识别其材质（如塑料、金属）、风格属性（如极简主义）并打上结构化标签。
*   **多维组合检索**：前端上线高级筛选面板，支持标签组的交集/并集检索。
*   **语义与向量检索**：接入 CLIP 模型与 Milvus 数据库，实现前端自然语言“以意搜图”（例如搜索“具有参数化纹理的纯白家电外观”）。

---

## 5. 预期收益

1. **打破信息孤岛**：将设计师散落在各自硬盘和收藏夹中的灵感资产进行集中化管理，提升跨组别协同效率。
2. **沉淀工业美学规范**：通过统一的 AI 标签体系与分类管理，帮助部门沉淀一致的设计语言与材质库标准。
3. **消除重复劳动**：自动化取代人工每日巡站看图的习惯，AI 自动打标取代人工归类，将团队的时间释放到核心的创意与设计输出上。